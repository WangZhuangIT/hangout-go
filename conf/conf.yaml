dev:
  database:
    host: localhost
  kafka:
    - host: calvin
      topic: yukon
      groupid: groupid
pro:
  database:
    host: 192.168.1.1
  kafka:
    - host: calvin
      topic: yukon
      groupid: groupid


